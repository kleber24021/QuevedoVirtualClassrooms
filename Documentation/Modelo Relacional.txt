USER(PK(USERNAME), HASHED_PASSWORD, NAME, SURNAME EMAIL, PROFILE_IMAGE_URL, FK(USER_TYPE))

USER_TYPE(PK(ID), NAME)

RESOURCE(PK(UUID_RESOURCE), RESOURCE_NAME, RESOURCE_URL, TIMESTAMP, FK(UUID_CLASSROOM), FK(RESOURCE_TYPE))

RESOURCE_TYPE(PK(ID), NAME)

CLASSROOM(PK(UUID_CLASSROOM), NAME, COURSE, FK(ADMIN_ID))

COMMENT_RESOURCE(PK(COMMENT_ID), TEXT, TIMESTAMP, FK(ID_RESOURCE) IS_ANSWER, FK(ANSWERS_TO)) Es una foreign key de otro comentario, por si acaso es una respuesta

USER_VISUALIZE_RESOURCE (PK(USERNAME, UUID_RESOURCE), VISUALIZATION_TIME, TIMESTAMP)

STUDENT_MEMBEROF_CLASSROOM(PK(FK(UUID_CLASSROOM), FK(USERNAME)))
